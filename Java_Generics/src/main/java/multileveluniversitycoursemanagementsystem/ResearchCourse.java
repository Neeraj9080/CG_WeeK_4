package multileveluniversitycoursemanagementsystem;
// Creating class ResearchCourse that extends CourseType
public class ResearchCourse extends CourseType
{
    // Parameterized constructor of the class
    public ResearchCourse (String courseName, String courseDuration, double coursePrice)
    {
        super(courseName, courseDuration, coursePrice);
    }
}
